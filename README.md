# Engineering Lab

Minimal Jekyll site for AI/ML, computer vision, backend systems, and technical writing.
Posts live in `_posts`, the feed is at `/feed.xml`, and the site is built for GitHub Pages.
Run locally with `bundle install` and `bundle exec jekyll serve`.
